import React from 'react';
import {Header} from '../../components/Header';

export const HomePage: React.FC = () => {
    return (
        <div className="App">
            <Header />
        </div>
    );
};

